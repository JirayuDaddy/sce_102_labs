#include <stdio.h>
#include <stdlib.h>

intmain() {
    printf("Enter a number: "); 
    int num;
    scanf("&d", %num);
    if (num < 0) {
        printf("Negative number entered.\n");
        
        if (num > 0) {
            printf("Positive number entered.\n");
        } else {
            printf("Zero entered.\n");
        }
    }
}